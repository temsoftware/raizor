<div class="table">
  <table class="mytable">
    <tr>
      <th class="spec">Selecione um status</th>
    </tr>

  <%form_for debtor do |status|%>
    <tr>
      <th class="spec">
        <%=status.select :status_id, 
                          Status.find(:all, 
                                    :conditions =>  ["profile_id >= ?", 
                                    session[:id]]).collect { |f| [ f.description, f.id ] }, {}, { :class => "input" } %>
      </th>
    </tr>

    <tr>
      <th class="spec">Selecione uma data</th>
    </tr>
 
    <tr>
      <th class="spec"><%=calendar_date_select_tag "data" %>  </th>
    </tr>
       
    <tr>
      <th class="spec">Digite uma observação</th>
    </tr>

    <tr>
      <th class="spec"><textarea name="obs" class="observacao"></textarea></td>
    </tr>
       
    <tr>
      <th class="spec"><button type="submit" class="botao">Atualizar cobrança &raquo;</button></th>
    </tr>
  <% end %>
  </table>
</div>

<div class="table">
  <table class="mytable">
    <tr>
      <th class="spec2">Pergunte ao Supervisor</th>
    </tr>
  <tr>      
      <th class="spec">
        <ul id="lista_posts">      
         <%=render :partial => "posts", :locals => {:post => debtor.post}%>
        </ul>
      </th>
</tr>
        <tr>
          <th class="spec"> 
            <% remote_form_for(:post, :url => posts_path) do |f| %>
              <%=f.hidden_field :debtor_id, :value => debtor.id%>
              <%=f.hidden_field :user_id,   :value => session[:id]%>
              <%=f.text_field :mensage, :value => ""%>
              <%=f.submit "Enviar comentário", :class => "botao"%>
            <% end %>
          </th>
        </tr>
  </table>
</div>
