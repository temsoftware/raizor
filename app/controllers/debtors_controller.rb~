class DebtorsController < ApplicationController
  def index
    @debtors = Debtor.find :all, :conditions => ["user_id = ?", session[:id]]
  end

  def show
    @debtors = Debtor.find params[:id]
  end
end
